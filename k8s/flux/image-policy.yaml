apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: hello-world
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: hello-world
  filterTags:
    pattern: '^\d{14}-[a-f0-9]{8}$'
    extract: '$1'
  policy:
    alphabetical:
      order: asc
